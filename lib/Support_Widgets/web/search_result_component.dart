import 'package:flutter/material.dart';

class SearchResultComponent extends StatelessWidget {
  const SearchResultComponent({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
